import React from 'react';

const ContactItem = ({item}) => {
    return (
        <div key={item.id}>
            <hr />
            <div>
                
            </div>
            <hr />
        </div>
    );
};

export default ContactItem;